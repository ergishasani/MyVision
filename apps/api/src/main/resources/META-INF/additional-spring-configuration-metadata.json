{
  "properties": [
    {
      "name": "jwt.secret",
      "type": "java.lang.String",
      "description": "Secret key used to sign JWT access tokens (HS256). Must be long and random in production."
    },
    {
      "name": "jwt.expiration-ms",
      "type": "java.lang.Long",
      "description": "JWT access token lifetime in milliseconds.",
      "defaultValue": 86400000
    }
  ]
}
