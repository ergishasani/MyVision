import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function TaxesReportPage() {
  return (
    <SectionPlaceholder
      title="Tax report"
      description="Review VAT totals, tax breakdowns, reverse-charge invoices, and export-ready summaries."
    />
  );
}
