import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function EmailSettingsPage() {
  return (
    <SectionPlaceholder
      title="Email settings"
      description="Manage sender identity, verification emails, password reset emails, and invoice delivery templates."
    />
  );
}
