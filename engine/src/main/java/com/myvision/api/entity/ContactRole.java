package com.myvision.api.entity;

/** What kind of relationship a contact is. Mirrors the filter tabs on the contact list. */
public enum ContactRole {
  customer,
  supplier,
  partner,
  prospect
}
