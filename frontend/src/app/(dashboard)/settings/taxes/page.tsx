import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function TaxSettingsPage() {
  return (
    <SectionPlaceholder
      title="Tax settings"
      description="Define VAT rates, reverse-charge behavior, small-business rules, and country-specific defaults."
    />
  );
}
