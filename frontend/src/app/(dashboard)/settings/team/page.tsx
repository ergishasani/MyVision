import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function TeamSettingsPage() {
  return (
    <SectionPlaceholder
      title="Team settings"
      description="Invite teammates, manage roles, and review account access."
    />
  );
}
